#include "contoh_while.h"
#include <stdio.h>


void contohWhile() {
/* definisi variabel lokal */
   int a = 10;

   /* eksekusi perulangan While */
   while( a < 20 ) {
      printf("nilai a: %d\n", a);
      a++;
   }
}
